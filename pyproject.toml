[project]
name = "web-trainer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.1",
    "pip>=25.1.1",
    "pyinstaller>=6.14.1",
]
